interface:
  display_name: "Explain Diff"
  short_description: "Explain a change visually, in chat or as a self-contained HTML page"
  default_prompt: "Use $explain-diff to explain this change visually."
policy:
  allow_implicit_invocation: true
