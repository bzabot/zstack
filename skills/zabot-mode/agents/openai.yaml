interface:
  display_name: "Zabot Mode"
  short_description: "Build a written spec and prove it works"
  default_prompt: "Use $zabot-mode to build this spec."
policy:
  allow_implicit_invocation: false
