interface:
  display_name: "Grilling"
  short_description: "Stress-test an idea and write its final spec"
  default_prompt: "Use $grilling to turn this idea into a confirmed Markdown spec."
policy:
  allow_implicit_invocation: false
